<div class='container'>
    <h4 id='list-name'>My Gifts</h4>
</div>

<div class='sigin'>
    <div class="border-form-div">
        <h4 id='list-name'><strong>My Wishlist</strong></h4>
        <ul class="list-group">
            <% @requested_gifts.each do |gift| %>
            <li class="list-group-item">
                <span class=class="label label-danger"><%= link_to 'X', delete_my_gift_path(gift), method: :delete, class:"label label-danger" %></span>
                <%= gift.name  %></li>
            <% end %>
        </ul>
        <%= render 'wishlist', gift: @gift%>
    </div>

    <div id='list' class='jumbotron col-lg-6'>
        <table class="table table-hover table-striped table-condensed">
            <thead>
                <tr text-align=center>
                    <th>Claimed Gifts</th>
                    <th></th>
                    <th>Recipient</th>
                </tr>
            </thead>

            <tbody>
                <tr>
                    <% @claimed_gifts.each do |rgift| %>
                    <td><%= rgift.name.capitalize  %></td>
                    <td>for</td>
                    <td><%= rgift.requester.username.capitalize  %></td>
                    <% end %>
                </tr>
            </tbody>
        </table>
    </div>
</div>
